import { TabPlaceholder } from '@/components/ui/TabPlaceholder';

const TeamsScreen = () => (
  <TabPlaceholder
    message="Em breve, suas equipes estarão disponíveis aqui."
    title="Equipes"
  />
);

export default TeamsScreen;
